import { Box, styled } from "@mui/material";

export const BuyersContentLayout = styled(Box)(() => ({
  width: "70%",
  margin: "0 auto",
  display: "flex",
  flexDirection: "column",
  gap: "30px",
}));
