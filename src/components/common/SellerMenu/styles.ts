import { Box, styled } from "@mui/material";

export const SellerMenuLayout = styled(Box)(({ theme }) => ({
  backgroundColor: theme.palette.background.paper,
  padding: "40px 26px 44px 26px",
  borderRadius: "23px",
  width: "320px",
}));
