import { Box, styled } from "@mui/material";

export const ContentLayout = styled(Box)(() => ({
  display: "flex",
  alignItems: "flex-start",
  gap: "55px",
  marginBottom: "60px",
  width: "100%",
}));
